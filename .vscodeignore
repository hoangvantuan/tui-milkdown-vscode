# Source & build config
src/**
*.ts
tsconfig.json
esbuild.config.js
esbuild.harness.config.js

# Dependency-verification harness (dev-only, never shipped)
harness/**
out/harness/**

# Dependencies
node_modules/**
package-lock.json

# Git & CI
.git/**
.github/**
.gitignore
.gitnexus/**
.gitnexusignore

# AI/agent tools
.agent/**
.agents/**
.claude/**
.mcp.json
_bmad/**
_bmad-output/**
AGENTS.md
CLAUDE.md
skills-lock.json

# IDE
.vscode/**
.vscodeignore

# Docs & plans
docs/**
plans/**

# Build artifacts
*.vsix
.DS_Store
# Sourcemaps: production builds don't emit them, but stale `build:dev` output
# would otherwise ship (~44 MB, 30 MB of it the mermaid artifact)
out/**/*.map
