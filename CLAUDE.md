# CLAUDE.md

@AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `hoangvantuan/tui-milkdown-vscode`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using the default label strings. See `docs/agents/triage-labels.md`.

### Sóng worker song song

Công thức phóng worker `agy` dưới Orca, các bẫy đã dính, ranh giới sở hữu khi nhiều nhánh cùng sửa `main.ts`, và quy trình merge. See `docs/agents/parallel-waves.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
