| Command                 | Notes                           |
| ----------------------- | ------------------------------- |
| `git log | head -20`    | shows the first 20 commits      |
| run `sort | uniq` first | code span plus surrounding text |
| plain `backticks` only  | no pipe in this code span       |


After the table.